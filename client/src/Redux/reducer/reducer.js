

// Seteamos el estado inicial del reducer
const initialState = {

}

// Seteamos el reducer
export default function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}