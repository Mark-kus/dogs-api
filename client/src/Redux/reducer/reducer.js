

// Seteamos el estado inicial del reducer
const initialState = {

}

// Seteamos el reducer
const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducer;